# Security

NeuroPrompt Semantic Compiler is offline-first and does not execute generated prompts.

Report security issues privately to the maintainers before public disclosure.

Critical constraints such as `no_sudo`, `no_external_api`, `no_destructive_actions` and `stay_inside_project_root` are preserved and validated by the core.
